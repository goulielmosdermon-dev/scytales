.DS_Store
**/.DS_Store
*.dmg
.claude/

# Design-source dump (~2GB) — not part of the served site
# Leading / = repo root only (macOS ignorecase would otherwise skip scytales/assets)
/Assets/
/UI/
/Reference/
